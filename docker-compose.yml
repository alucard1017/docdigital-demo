version: "3.8"
services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: tu_password
      POSTGRES_DB: docdigital2
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
