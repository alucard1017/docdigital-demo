
Guarda el archivo.

## 2. Hacer commit en `dev`

Vuelve a Git CMD (si el servidor está corriendo, abre otra ventana o deténlo con `Ctrl + C`) y ejecuta:

```bash
cd C:\Users\Usuario\Desktop\docdigital
git status
git add backend/README.md
git commit -m "Agregar README específico para el backend"
git push
